# 任务完成检查
- 修改后运行：`pnpm lint`；必要时 `pnpm build` 验证构建。
- 确认未引入新的依赖或锁文件变更（需用户批准后才改 package.json/pnpm-lock.yaml）。
- 前端改动确保主要页面（Home、Workspace）可编译；如涉及动画或交互，手动验证关键路径（搜索、工作区切换）。
- 维持 UTF-8 编码，遵守简体中文交流；仅对需求相关部分改动，避免影响其他功能。
- 若无法完成或信息缺失，需向用户说明并请求补充。